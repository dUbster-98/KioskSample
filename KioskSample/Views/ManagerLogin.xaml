<UserControl x:Class="KioskSample.Views.ManagerLogin"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:behaviors="clr-namespace:KioskSample.Behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:KioskSample.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d"
             d:DesignWidth="800" 
             d:DesignHeight="450"
             prism:ViewModelLocator.AutoWireViewModel="True"
             FontSize="20">
    <Border>
        <Grid
            Width="400"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="LightGray">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock Margin="10" Text="관리자 로그인"/>
            <TextBlock
                Grid.Row="1"
                Margin="10"
                Text="ID"/>
            <TextBlock
                Grid.Row="2"
                Margin="10"
                Text="Password"/>
            <TextBox
                Grid.Row="1"
                Grid.Column="1"
                Margin="0,0,10,0"
                Padding="10,10,0,0"
                Text="{Binding ID, Mode=TwoWay}"/>
            <PasswordBox
                Grid.Row="2"
                Grid.Column="1"
                Margin="0,0,10,0"
                Padding="10,10,0,0">
                <b:Interaction.Behaviors>
                    <behaviors:PasswordBehavior BindingPassword="{Binding Password, Mode=TwoWay}"/>
                </b:Interaction.Behaviors>
            </PasswordBox>

            <Button
                Grid.Row="3"
                Grid.ColumnSpan="2"
                Margin="10"
                Content="Login"
                Command="{Binding LoginCommand}"/>
        </Grid>
    </Border>
</UserControl>
